spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pandawok
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update

application:
  security:
    jwt:
      secret_key: ${SECRET_KEY}
      expiration: 8640000

